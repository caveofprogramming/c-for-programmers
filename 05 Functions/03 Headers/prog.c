#include <stdio.h>
#include "funcs.h"

// TODO On Mac can compile with gcc *.c
// On Windows must name each file separately: gcc prog.c funcs.c
// Can avoid this using tools like make or, better, cmake.

int main() 
{
    greet();
    return 0;
}

